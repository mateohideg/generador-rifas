interface raffleDetailsType {
  quantity: number,
  organizationName: string,
  raffleName: string,
  firstPrize: string,
  secondPrize: string,
  thirdPrize: string,
  price: string,
  logo: File
};

interface Window {
  fonts: FontFaceSet
};
