interface raffleDetailsType {
  quantity: number,
  organizationName: string,
  raffleName: string,
  firstPrize: string,
  secondPrize: string,
  thirdPrize: string,
  price: string,
  logo: Uint8Array
};

interface Window {
  fonts: FontFaceSet
};
